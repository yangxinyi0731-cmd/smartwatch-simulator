# 项目持续状态

更新时间：2026-08-29

## 总目标

完成一个可在 Windows 电脑上直接打开的三模型模拟智能手表平台，并保留数据来源、模型卡、测试报告和可复现命令。

## 当前步骤

本轮完整交付已经完成：跌倒 ONNX 与 100 案例同源重放、100 天合成规律与统计规则、CAPTURE-24 腕部活动 ONNX 与参与者隔离留出评估、可恢复批量回放和 Windows 一键本地运行均已接入。SQLite 结构版本 5 的默认库现有 105 个案例、3 个研究模型 manifest、3 份规范化报告和持久化批量任务。只有必须由用户本人处理的登录、许可申请、隐私/法律选择或外部账号操作才暂停。

案例目录、真实回放、批量回放和统一测试报告已经形成三模型可用闭环：用户可筛选 105 个案例、读取三个已登记研究模型，回放真实六轴 WEDA、真实三轴 CAPTURE-24 或 100 天合成事件，并查看波形、标签、候选告警/活动概率、质量标记和模型证据。批量任务把进度、失败、恢复次数和三个模型各自的受限摘要写入 SQLite；测试报告从已保存 JSON 读取真实数值。普通使用者可双击 Windows 脚本，以同一个只监听 `127.0.0.1` 的进程打开前端、API 与 WebSocket。

## 本步已完成

- SQLite 从结构版本 4 迁移到版本 5，新增 `batch_replay_tasks` 与 `batch_replay_items`；任务保存幂等请求标识、案例集合 SHA-256、状态、进度、错误、当前案例、恢复次数和逐案例独立结果摘要；
- 新增 `POST /api/batch-replays`、`GET /api/batch-replays` 与 `GET /api/batch-replays/{task_id}`；同请求标识与同案例集合返回原任务，不重复执行，标识冲突返回安全错误；
- 后台任务单条失败后继续后续案例；整批异常会把未完成项收口到终态，不留下任务已失败但条目仍运行的矛盾状态；
- 服务启动会把中断的 `RUNNING` 条目恢复为 `PENDING`，已完成条目不重复执行，并递增恢复次数；
- 前端开放“可恢复批量回放”，按当前真实性筛选创建任务，显示真实进度、失败数、当前案例、重启恢复次数和三个模型独立完成数；
- FastAPI 在 `frontend/dist/index.html` 存在时由同一进程提供生产前端、API 与 WebSocket；开发模式仍可使用 Vite；
- 新增 `scripts/windows/start-smartwatch.cmd`、`stop-smartwatch.cmd` 和 `diagnose-smartwatch.cmd` 及对应 PowerShell 脚本；启动脚本自动构建、健康等待并打开页面，停止脚本核对 PID/进程路径/启动时间，诊断脚本执行只读依赖与健康检查；
- Windows PowerShell 脚本保存为带 BOM 的 UTF-8，已在系统 `powershell.exe` 中实际运行中文输出；运行状态与日志位于 Git 忽略目录；
- 服务版本提升到 `0.8.0`，生产页面固定为 `http://127.0.0.1:8000/`；
- 2026-08-29 再次只读核验 Open Wearables、WEDA-FALL、近期相似跌倒项目、Stable-Polynomial-Train、FastAPI 官方模板和 OxWearables/capture24 的 GitHub HEAD；FastAPI 模板 HEAD 已变化，记录已更新，没有复制其代码、指标或权重；

- 从用户自有跌倒模型仓库固定提交 `6c8bd6058f19467221e292c77a8df00630b8bd0b` 核验并导入 ONNX，模型 SHA-256 为 `1e214ebd89fce6620cf09a7a071aa904cd5ca0932e960181ce43c3695e212a3f`；
- ONNX Runtime `1.29.0` 在 Windows CPython 3.14 上实测输入 `float32[batch,200,6]`、输出 `fall_probability[batch]` 与 CPU 执行提供器；
- 100 个同源案例逐窗重放行为核验：40 个受控模拟跌倒事件均出现重叠告警，60 个日常活动中 2 个出现误报告警段，老人 30 个日常活动本次 0 个误报告警段；报告明确说明这不是独立泛化评估；
- 固定种子 `20260828` 生成 100 天、551 条 `SYNTHETIC_ROUTINE` 用餐/午睡/散步事件；
- 规律模型独立实现 IQR 稳健过滤、时间/时长区间和每日次数规则，没有复制许可证不明的用户压缩包源码；
- 基线、晚餐过晚、缺少用餐、散步过多和午睡过长五个规则场景全部通过；异常分数明确不是医学风险概率；
- SQLite 结构版本 4 新增 `routine_profiles` 与 `routine_events`，运行库实数为 100 个 WEDA-FALL 来源案例加 1 个合成规律案例；
- 已注册跌倒、规律与活动三个 manifest，三者均为 `deployment_approved=false`；
- CAPTURE-24 数据许可证已由 2024 年 Scientific Data 论文核验为 CC BY 4.0；工具代码为单独学术许可，本项目不复制；
- 活动管线固定 `20 秒 × 20 Hz × 3 轴`，对 100 Hz 原始腕部加速度先做 5 点箱形低通再降采样，并按不重叠参与者分组训练/评估；
- 官方 CAPTURE-24 6,902,652,480 字节大文件下载因服务器连接中断而失败；只从 2,220,746,092 字节前缀恢复出 48 个完整参与者成员，恢复 ZIP 为 2,348,094,346 字节、SHA-256 `e1d44ba9bd6cb12f7d0e4dc2cbb0be56ce00182de7fbcb070907b8bf36fff4ba`，逐成员通过解压长度与 CRC-32 核验；不得称为完整 151 人数据包；
- 标签可用性扫描按恢复目录固定顺序实际扫描 43 人，前 24 名四类均至少 20 窗口者固定为训练组，随后 12 名固定为评估组；名单选择不运行模型、不比较成绩，组间重叠为 0；
- 活动模型实际使用 7,240 个训练窗口和 3,728 个评估窗口；参与者留出准确率 `0.6075643776824035`、宏平均 F1 `0.5977935900949525`，完整混淆矩阵与逐类指标已保存；这是恢复子集内窗口级评估，不是老人专项或独立外部验证；
- 活动 ONNX SHA-256 为 `47a552b23092920ffbd74c14f3de3abce2aad33771066180f31a42f6e6be667e`，与 NumPy 在完整评估矩阵上的最大绝对误差为 `3.337860107421875e-06`；
- 新增 `GET /api/models` 和 `GET /api/cases/{case_id}/replay-preview`；接口从 SQLite 重建并校验案例合同，读取传感器文件时重新核对安全相对路径、SHA-256、数组形状、数据类型和有限值；
- 回放预览对 WEDA 案例实际运行固定跌倒 ONNX，返回逐窗概率、manifest 阈值、合并候选告警段、真实标签和数据质量；对合成规律案例返回 100 天、551 条事件及每日三项独立规则判断；
- 前端真实分页读取全部 105 个案例，按五种真实性类别筛选，并显示三个已登记研究 manifest；
- 前端实现开始、暂停、继续、重置、1×–16×速度、真实进度、六轴分组波形、标签/告警时间线、质量标记、规律逐日结果和明确真实性说明；没有随机波形或综合风险分数；
- 新增 `GET /api/reports` 与 `GET /api/reports/export.json`，规范化跌倒同源重放和合成规律规则核验报告，并为实际报告文件重新计算 SHA-256；
- 前端开放“测试报告”导航，逐模型显示证据范围、实测值、解释、限制和哈希，提供统一 JSON 下载；同源重放、规则场景与未来参与者留出评估不会混称为外部验证；

- 只读核验 WEDA-FALL 本机检出为固定提交 `74e0b93cb061d4ecbca12628f2d47090e97fbeea`，并要求已跟踪文件无改动；
- 导入器固定处理代码来源提交 `6c8bd6058f19467221e292c77a8df00630b8bd0b`，独立实现相同的排序、重复时间戳平均和公共 50 Hz 时间网格对齐；
- 按确定性平衡规则选择 100 条不同来源记录：40 条年轻参与者受控床垫模拟跌倒、30 条老年参与者受控日常活动、30 条年轻参与者受控日常活动；
- 每条记录均包含真实加速度与陀螺仪，没有添加全零或伪造陀螺仪；
- 处理后 100 个 NPY 文件均至少 200 行、固定六列 `ax/ay/az/gx/gy/gz`，并保存在 Git 忽略目录 `data/processed/`；
- `data/catalog/weda_fall_100_v1.json` 保存固定来源、选择策略、100 个不同来源指纹、原始文件哈希、处理文件哈希、采样信息、质量标记和标签；
- WEDA-FALL 许可状态保持 `UNVERIFIED`、是否可重新分发保持未知；原始与处理文件均不提交 Git；
- 100 条记录均如实保留原始时间轴间隙质量标记；38 条还保留至少一个传感器有效回调频率偏离名义 50 Hz 超过 10% 的标记；
- SQLite 从结构版本 2 迁移到版本 3，新增 `import_runs`、`case_import_runs`、`case_source_files`、`sensor_quality` 和 `ground_truth_events`；
- 导入在一个数据库事务内完成：等价批次可安全重跑，复用标识但内容冲突会整批回滚；
- 默认运行数据库已实测写入 105 个案例（100 个 WEDA、1 个合成规律、4 个 CAPTURE-24 活动窗口），并注册三个研究模型；资产同步连续执行两次后数量保持不变；
- 服务版本现为 `0.8.0`，`GET /api/health` 实测返回结构版本 5 和案例实数 105；
- 以下上一大关成果继续保留：
- 新建 `backend/app/contracts.py`，固定五种真实性类别和三个模型的独立职责；
- 固定跌倒模型 `50 Hz × 4 秒 × 6 轴 = 200×6`，缺少真实陀螺仪的案例会被合同拒绝；
- 固定规律异常模型为 100 天合成的用餐、午睡、散步事件，不描述为真实老人训练；
- 固定活动识别模型 `20 Hz × 20 秒 × 3 轴 = 400×3` 和四类活动候选；
- 为案例、来源、传感器窗口、model manifest、三个独立模型输出和五类回放事件建立 Pydantic 合同；
- 合同拒绝不安全相对路径、形状/单位不一致、概率和不一致、阈值判断漂移、派生案例无父关系和未完成外部验证却通过部署审批；
- SQLite 从结构版本 1 迁移到版本 2；
- 新建 `data_sources`、扩展 `cases`，并新建 `sensor_streams`、`model_manifests`、`replay_sessions`、`replay_events`、`model_outputs`；
- 真实存在的 v1 案例迁移时保留原值，缺失字段明确标记为 `UNKNOWN / UNVERIFIED / legacy-unrecorded`，不编造来源；
- 旧版派生案例缺少父关系时只作为禁止进入模型的 legacy 记录保留；新合同仍强制所有新派生案例记录父案例；
- 新建 `GET /api/contracts`，返回合同版本 `1.0.0`、三模型合同和真实性硬边界；
- 新建 `GET /api/cases`，提供参数绑定搜索、真实性/适用模型筛选、白名单排序、服务端分页和越界页码收敛；
- 案例库失败返回不含内部堆栈的可重试 503；
- 合同生成链继续覆盖 OpenAPI、领域 JSON Schema 和前端 TypeScript；
- 新建 OpenAPI 与领域 JSON Schema 生成脚本，并增加提交快照漂移测试；
- 使用 `@hey-api/openapi-ts@0.99.0` 生成前端 TypeScript 类型；生成器输入只使用本地 JSON；
- 使用 npm override 将 `js-yaml` 固定为 `4.3.1`，npm audit 恢复为 0 个已知漏洞；
- 检索并固定 RADAR-base、Open mHealth、AWARE、FastAPI 官方模板、WEDA-FALL、OxWearables 和近期相似端到端跌倒项目的 Git HEAD 与许可状态；
- 新建 `docs/OPEN_SOURCE_REFERENCE_REVIEW.md`，明确采用与排除理由；没有复制这些项目的指标、权重或数据。

## 本步验证结果

- `.\.venv\Scripts\python.exe -m pytest .\backend\tests -q`：59 个测试通过；
- 数据库测试覆盖新库建表、v1→v2 保留迁移、旧版派生记录、来源许可缺失、服务端分页、筛选、LIKE 通配符转义和结构版本保护；
- 合同测试覆盖三模型输入、陀螺仪缺失、路径逃逸、派生父关系、六轴形状、概率和、阈值判断、最高概率标签、artifact 路径和部署审批；
- 提交的 OpenAPI 与领域 JSON Schema 均与当前代码生成结果一致；
- `npm --prefix frontend run generate:api`：成功生成 2 个 TypeScript 文件；
- `npm --prefix frontend run build`：成功；Vue 类型检查和 Vite 生产构建均通过；
- 当前构建产物为 CSS 559.48 kB（gzip 72.61 kB）、JavaScript 113.09 kB（gzip 41.19 kB）；
- `npm --prefix frontend audit`：0 个已知漏洞；
- `.\.venv\Scripts\python.exe -m pip check`：没有损坏或冲突的依赖；
- `designmd lint DESIGN.md`：0 个错误、0 个警告、1 条令牌汇总信息；
- Premium 严格审计：0 个 findings、0 个错误、0 个警告、0 个未解决项；
- 运行态 HTTP 实测：服务版本 0.8.0、SQLite 结构版本 5、案例总数 105、模型清单与报告清单各 3 个，均未获部署批准；
- 生产一键服务整批实测 105 个案例全部完成、失败 0：跌倒 100/100、规律 1/1、活动 4/4；同一请求再次提交返回 HTTP 200 和原任务 ID；
- 真实恢复实测：手动让任务停在 `RUNNING` 后关闭并重启服务，任务最终 `COMPLETED`、完成 1/1、失败 0、`recovery_count=1`；
- Windows 一键脚本实测完成“启动、健康等待、重复启动复用、运行中诊断、停止、停止后诊断、重新启动”；停止后端口不可达且状态文件清理，数据库与日志保留；
- 端口实测只监听 `127.0.0.1:8000`，没有监听 `0.0.0.0` 或局域网地址；
- 对 100 个本机 NPY 逐一重算 SHA-256，全部与目录一致；100 个案例 ID 与来源指纹均不重复，全部为六轴；
- 同一真实导入命令连续执行两次，案例数仍为 100，目录哈希保持 `4e6a68fa8de730999d85f2d254af40adc6241038fc2af2a0c2ce9cfab4041f49`；
- 右侧本地前端持续运行并显示 SQLite 结构版本 5、105 个案例和三个模型；
- 浏览器实测 1440×900 与 390×844 均无页面级横向溢出；真实性筛选可切换到合成规律案例；4×回放在约 1.1 秒内由 00:00 走到 00:05，暂停保持游标，重置回到 00:00，控制台无错误；
- 报告页面运行态实测读取 3 份报告，显示 `40 / 40` 受控模拟跌倒重叠告警、`2` 段日常活动误报、`0 / 30` 老人日常活动误报告警段、`5 / 5` 确定性规则场景，以及活动参与者留出指标；相邻文字明确限定含义；
- 活动页面运行态实测读取 400 个真实三轴点，显示进食候选固定窗口的四类概率和来源映射标签；活动报告显示 3,728 个留出窗口、60.8% 准确率与 0.598 宏平均 F1，并明确“恢复前缀子集、不是完整 151 人、不是老人数据、不是外部验证”；
- 浏览器实测桌面 1440×900 和手机 390×844 均无页面级横向溢出，控制台无错误或警告；活动案例不再显示跌倒专用概率/阈值文案；
- 一键版生产页面再次实测桌面 1440×900 和手机 390×844，无页面级横向溢出、无控制台错误或警告，并显示最新 105/105 批量完成状态；
- Google Chrome 已安装，但 ChatGPT/Codex Chrome 控制扩展仍未安装；不能把 Codex 应用内 Chromium 检查写成 Chrome 专项验收。

## 明确未完成

- WebSocket 仍只传递系统状态，不传感器或模型数据；
- 左侧“案例库”独立页面仍未开放；当前案例选择、筛选、单案例回放和批量回放都在总览页完成；
- 没有真实设备接入、医疗诊断或正式救援能力；
- 没有创建或推送 GitHub 远程仓库。

## 当前运行命令

普通使用：双击 `scripts\windows\start-smartwatch.cmd`，页面地址为 `http://127.0.0.1:8000/`；停止与诊断使用同目录对应 `.cmd`。

开发模式第一个 PowerShell 窗口：

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

开发模式第二个 PowerShell 窗口：

```powershell
npm --prefix frontend run dev
```

开发前端默认地址：`http://127.0.0.1:5173/`；一键生产地址：`http://127.0.0.1:8000/`；健康检查：`/api/health`；合同：`/api/contracts`；案例：`/api/cases`；模型：`/api/models`；报告：`/api/reports`；报告下载：`/api/reports/export.json`；单案例回放：`/api/cases/{case_id}/replay-preview`；批量回放：`/api/batch-replays`。

## 后续可选扩展

本轮授权范围内的平台、三模型、批量回放和 Windows 本地交付已经完成。GitHub 远程发布、真实设备、外部数据许可申请、登录或正式发布仍需用户本人授权或操作。任何后续扩展继续保持：

- WEDA-FALL 跌倒是年轻参与者在受控条件下模拟；
- WEDA-FALL 老人只执行日常活动，用于误报分析；
- 合成规律必须标记 `SYNTHETIC_ROUTINE`；
- 不能满足六轴输入的记录不进入跌倒模型；
- 许可未核验的数据不重新分发或商用；
- 不凑数、不生成随机波形、不编造模型指标。

如果公开数据下载需要用户本人申请、同意条款或登录，只暂停相应数据源，继续推进不需要外部授权的部分。
