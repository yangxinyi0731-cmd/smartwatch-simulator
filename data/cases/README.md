# 统一测试案例

后续案例按 `backend/app/contracts.py` 和 `docs/contracts/domain-contract.schema.json` 保存，并明确区分：

- `REAL_FREE_LIVING`：真实自由生活记录；
- `REAL_LAB_ACTIVITY`：真实参与者受控活动；
- `SIMULATED_FALL`：年轻参与者在受控条件下模拟的跌倒；
- `SYNTHETIC_ROUTINE`：程序生成的生活规律；
- `DERIVED_PERTURBATION`：有父案例与处理命令的明确派生异常。

当前没有测试案例。
