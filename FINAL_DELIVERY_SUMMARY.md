# 老年人 AI 模拟智能手表平台：最终交付要点

更新时间：2026-08-29

## 最终地址

- 平台首页：<http://127.0.0.1:8000/>
- 批量回放：<http://127.0.0.1:8000/#batch-replays>
- FastAPI 接口文档：<http://127.0.0.1:8000/docs>
- 健康检查：<http://127.0.0.1:8000/api/health>

上述地址只在运行平台的这台 Windows 电脑上访问，不向局域网或互联网开放。现场复核显示服务仍在运行：FastAPI `0.8.0`、SQLite 结构版本 `5`、案例 `105` 个，状态为 `ready`。

## 最简单的运行方法

打开项目的 `scripts\windows` 文件夹：

1. 双击 `start-smartwatch.cmd`：自动构建前端、启动本机服务并打开页面；
2. 双击 `stop-smartwatch.cmd`：安全停止本项目进程，保留 SQLite 数据与日志；
3. 双击 `diagnose-smartwatch.cmd`：只读检查 Python、Node.js、依赖、前端构建、数据库、三个模型资产和健康接口。

首次换电脑运行前，仍需按根目录 `README.md` 的“首次安装”安装 Python 与前端依赖。

## 当前已完成

- Vue 3 + TypeScript + Vite + Tabler 中文前端；
- 基于参考截图完成“静谧健康研究台”整体风格迭代，并建立可复用 `DESIGN.md`；
- FastAPI 本机后端；
- SQLite 持久化数据库；
- WebSocket 系统连接状态；
- 105 个可追溯案例；
- 三个独立研究模型；
- WEDA-FALL 真实六轴回放；
- CAPTURE-24 真实三轴活动窗口回放；
- 100 天合成生活规律回放；
- 模型证据、数据质量、标签和候选告警时间线；
- 三份带范围、限制、路径和 SHA-256 的测试报告；
- SQLite 持久化、幂等、可恢复的批量回放；
- Windows 一键启动、停止和诊断；
- 同一 `127.0.0.1:8000` 进程提供生产前端、API 与 WebSocket。

## 最新 UI 设计系统

当前界面已从基础 Tabler 工作台迭代为统一的“静谧健康研究台”，不是另建 V2，也没有改变数据、模型或 API：

- 主色为健康深绿 `#0B7D4D`，辅助色为清澈蓝 `#4FB9DC`；页面使用冷灰白 `#F5F7F6`，主卡片为白色；
- 主文字为深海军蓝 `#253247`，次文字为蓝灰色 `#66758D`；标题使用离线衬线字体栈，正文和控件使用简体中文系统无衬线字体；
- 页面主标题采用蓝到薄荷绿的健康光谱渐变，浮动白色导航、24px 主卡片圆角、18px 内部分组圆角和轻羽化阴影贯穿全页；
- 桌面顶栏单行展示；不大于 1180px 时变成品牌/操作与导航两行；手机端只有导航条自身横向滚动，页面不横向溢出；
- 三模型中心在桌面三列、窄屏两列、手机单列；回放、来源、批量任务和测试报告沿用同一组件语言；
- 手机端所有当前可操作控件至少 44px 高，键盘焦点、原生选择器、滚动条、forced-colors 和 reduced-motion 均保留明确规则；
- `frontend/src/style.css` 是运行时设计令牌来源，`DESIGN.md` 记录颜色、字体、字号、行高、间距、圆角、阴影、组件和响应式规范；以后新增或调整页面必须先读取并复用该规范。

本轮只吸收参考图的配色、字体层级、留白、卡片、按钮和布局语言，没有复制下载按钮、用户评价、活跃人数或健康功效宣传，也没有增加任何虚构指标。

## 最新批量回放实测

- 总案例：`105`
- 完成：`105`
- 失败：`0`
- 腕部跌倒检测：`100 / 100`
- 个人规律异常：`1 / 1`
- 腕部活动识别：`4 / 4`

还完成了一次真实服务重启恢复验证：任务在 `RUNNING` 状态中断，重启后自动恢复并完成，最终完成 `1 / 1`、失败 `0`、`recovery_count=1`。

批量任务不会把三个模型的结果相加或平均，也不会生成综合医学风险。

## 三个模型的真实性边界

### 腕部跌倒检测

- 输入固定为 `50 Hz × 4 秒 × 6 轴 = 200 × 6`；
- 40 个跌倒案例来自年轻参与者在受控床垫条件下模拟；
- 老年参与者只执行日常活动，用于误报分析；
- 同源行为重放中，40/40 受控模拟跌倒出现重叠候选告警，60 个日常活动出现 2 段候选误报，30 个老人日常活动本次为 0 段；
- 这些结果不是独立外部泛化成绩，不能描述成真实老人跌倒验证。

### 个人生活规律异常

- 固定种子 `20260828`；
- 100 天、551 条用餐/午睡/散步合成事件；
- 真实性固定为 `SYNTHETIC_ROUTINE`；
- 五个确定性规则场景全部通过；
- 规则偏离不是疾病概率、医学风险或真实世界准确率。

### 腕部活动识别

- 输入固定为 `20 Hz × 20 秒 × 3 轴 = 400 × 3`；
- 使用 7,240 个训练窗口、3,728 个评估窗口；
- 24 名训练参与者、12 名不重叠评估参与者；
- 恢复子集内参与者留出准确率为 `0.6075643776824035`，宏平均 F1 为 `0.5977935900949525`；
- 官方大文件下载中断后，只恢复出 48 个完整参与者成员，不是完整 151 人数据包；
- CAPTURE-24 以年轻参与者为主，不是老人专项数据；
- 上述结果不是独立外部验证。

三个模型均保持 `deployment_approved=false`。本平台是研究和比赛演示原型，不是医疗器械、诊断系统或正式救援系统。

## 最终验证结果

- 后端自动测试：`59 passed`；
- 前端 Vue/TypeScript 类型检查：通过；
- Vite 生产构建：通过；
- npm audit：`0 vulnerabilities`；
- Python pip check：无损坏或冲突依赖；
- OpenAPI、JSON Schema、前端 TypeScript 生成链：通过；
- designmd lint：0 错误、0 警告；
- Premium 严格界面审计：0 findings；
- 桌面 `1440×900`：无页面级横向溢出；
- 平板 `1024×768`：浮动顶栏正确换为两行，状态区正确变为两列，无页面级横向溢出；
- 手机 `390×844`：无页面级横向溢出；
- 手机横屏 `844×390`：无页面级横向溢出；
- `390×844` 下导航只在自己的条带中滚动，所有当前可操作控件均不小于 44px；
- 三模型中心、测试报告和长限制说明在窄屏下无裁切；
- 案例筛选、原生选择器、开始、暂停、继续状态和重置交互链：通过；
- 页面语义检查：1 个 `main`、1 个主导航、1 个 `h1`、无重复 ID、所有页内锚点均有真实目标；
- 生产页面浏览器控制台：0 错误、0 警告；
- 服务端口只监听 `127.0.0.1:8000`；
- Windows 启动、重复启动复用、诊断、停止、停止态诊断和重新启动：全部实测通过。

## Git 状态

- 分支：`codex/tabler-ui-migration`
- 本轮 UI 迭代基线：`08ba0ad docs: add final delivery summary`
- 本轮最新提交主题：`feat: apply serenity-inspired design system`；准确提交号以 `git log -1 --oneline` 的现场输出为准。
- 关键提交：
  - `08ba0ad docs: add final delivery summary`
  - `af2e355 feat: add Windows one-click delivery`
  - `dc5b4e1 feat: add recoverable batch replays`
  - `d637c6a feat: complete traceable activity model`
- 本项目目前没有配置 Git remote，也没有推送到 GitHub。

GitHub 发布属于外部账号与公开发布操作，不能自动执行。WEDA-FALL 许可仍为 `UNVERIFIED`；原始数据、处理数组、运行数据库和大体积恢复数据均不进入 Git，也不能因为发布代码就擅自重新分发。

## 有意保留在范围外的内容

- 左侧独立“案例库”页面尚未开放，但总览页已有筛选、单案例和批量回放；
- WebSocket 当前只传系统状态，不伪装成真实设备传感器流；
- 没有接入实体手表或真实个人健康数据；
- 没有真实老人跌倒外部验证；
- 没有医疗诊断或正式救援能力；
- 没有向 GitHub 或互联网发布项目。

## 项目与文档位置

- 项目目录：`C:\Users\yangxinyi\Documents\Codex\2026-08-27\smartwatch-health-simulator`
- 普通使用说明：`README.md`
- Windows 一键说明：`scripts\windows\README.md`
- 当前完整交接：`CONTEXT_HANDOFF.md`
- 实测状态：`TASK_STATE.md`
- 数据与模型真实性声明：`DATA_AND_MODEL_NOTICE.md`
- 开源参考复核：`docs\OPEN_SOURCE_REFERENCE_REVIEW.md`
- 系统架构：`docs\ARCHITECTURE.md`
