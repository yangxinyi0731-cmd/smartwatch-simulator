# P0+P1+P2 第一大关 Gate 决定

## 1. 结论

工程状态：`PASS_E0_FOUNDATION`

允许表述：P0 产品/目标合同、P1 数据/标签合同和 P2 确定性离线研究骨架可重复运行。

不允许表述：这不证明真实老人提前预测能力，不授权接入真实个人数据、进入 P3、训练提前预测模型、上线风险 UI、提示用户、通知家属或部署。

正式签署：`PENDING_EXTERNAL`。产品、研究和安全/伦理负责人均未由代码任务代签。

## 2. P0 结果

- 合同必填完整率：100%；
- 目标事件：8 类，互斥定义已冻结；
- 时间锚点：5 个；
- 强制指标：11 项；
- 数秒级评估时域：1/2/3/5/10 秒；
- 规范化合同 SHA-256：`4f796983479cdedb5795189834d7f658599791a515396a33942f27dee6ab62b0`；
- 新 E0 交付物中的越级能力断言：0。

## 3. P1 结果

- 原始不可变层和规范化派生层合同已建立；
- 参与者、地点、设备和会话混淆会被拒绝；
- 乱序、重复、错误单位、缺失通道、非有限数、路径逃逸和伪六轴会被拒绝；
- 数秒级同步误差上限固定为 100 ms；
- 双人裁决、置信等级、事件锚点和撤回索引合同已建立；
- JSON Schema 与代码无漂移；
- Schema SHA-256：`70114fccaf664be3c4412653dfe11ebf177e7fdfbedd0abb6905e0ee0eb38f72`。

## 4. P2 结果

- 审计 11 个现有数据、模型和报告资产，覆盖率 100%；
- CAPTURE-24 保存拆分参与者交叉：0；
- 严格事件前截断泄漏 finding：0；
- 确定性夹具重复运行两次，规范化结果哈希一致；
- 夹具规范化 SHA-256：`02f97ab1d6d2fff4beab0553a69d5ff42329a7aa0ff4a28e7a78d9f78d9ff609`；
- dry-run 状态机真实外部通知数：0；
- 当前数据审计报告 SHA-256：`b23c3c34d35c3e11a527259b2b5b00c9108b18ce455f2685d9bfc5b5294a2873`；
- 确定性夹具报告文件 SHA-256：`7b7d77f4f981b2f1264246e6603a5997aef5e5c29dfa319d4417c0fa23d0f04b`。

夹具中显示的召回、精确率、AUPRC、校准、覆盖率和误报/人日只是公式金标准，不是实际模型成绩。

## 5. 回归验证

- 后端原有测试与新研究测试合并：86 passed；
- 新研究包局部测试：27 passed；
- 前端 Vue/TypeScript 与 Vite 生产构建：通过；
- npm audit：0 vulnerabilities；
- pip check：无损坏或冲突依赖；
- Markdown 检查：0 问题；
- 合同/Schema 漂移：0；
- `git diff --check`：无空白错误，仅有 Windows LF→CRLF 提示。

具体命令和结果见 `docs/early_risk/REPRODUCIBILITY.md` 与 `reports/early_risk/e0/verification.json`。

## 6. 下一 Gate

本轮在 P2 停止。进入 P3 至少需要：

1. 产品、研究和安全/伦理负责人正式签署目标合同；
2. 用户另行明确授权；
3. 伦理、知情同意、数据控制、撤回/删除和许可方案；
4. 不把批准原件、个人映射、视频、密钥或真实运行数据加入 Git。
