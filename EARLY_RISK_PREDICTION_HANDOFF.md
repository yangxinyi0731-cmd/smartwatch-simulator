# 老年人 AI 模拟智能手表：提前风险预测方向交接

更新时间：2026-08-31

实际仓库：`C:\Users\yangxinyi\Documents\Codex\2026-08-27\smartwatch-health-simulator`

文档性质：未来产品与研究方向的自包含交接；本文件不代表已实现、已训练、已部署或已验证该能力。

## 0. 一页结论

用户真正要做的产品核心，不是“检测到已经摔倒后再报警”，而是：

> 学习每位老人的长期个人生活习惯和身体活动基线，持续识别当下活动、规律偏离与短时动作前兆，在证据充分时估计未来数秒内发生危险事件的风险，争取在撞击或失稳完成前发出可操作的预警。

当前仓库已经完成的是一个本机三模型研究演示平台：它能分别回放并展示腕部跌倒候选检测、合成生活规律异常和腕部活动识别，也能持久化批量回放、报告和证据。它**尚不具备**前瞻性的数秒级跌倒预测能力，不能把当前三个模型的输出相加、平均或换名后宣称为“未来跌倒概率”。

未来方向必须分成两类时间尺度：

1. **长期背景风险**：从个人纵向基线、近期规律偏离、活动量变化、睡眠/进食/步态相关代理信号等判断“今天或当前时段是否更脆弱”；
2. **数秒级即时风险**：从连续腕部信号、活动状态、姿态变化和失稳前动作模式中判断“接下来若干秒是否接近危险事件”。

背景风险可以调整监测敏感度和交互策略，但不能单独触发紧急报警。数秒级即时风险必须有连续、及时、可校准的证据，并经过真实前瞻数据和外部验证，才能进入真实预警试验。

## 1. 本交接的边界

本文件只完成方向定义、现状盘点、数据与评估路线、报警原则和下一任务入口，不实施模型、接口、数据库迁移、前端、训练、部署或真实通知。

永久表述边界：

- 当前平台是研究与比赛演示原型，不是医疗器械、诊断系统或正式救援系统；
- “争取提前数秒”是未来研究目标，不是已实现承诺，也不保证每次危险都存在可观测前兆；
- 当前跌倒模型的正样本来自年轻参与者在受控床垫条件下的模拟跌倒，不是真实老人意外跌倒；
- 当前规律模型只识别合成生活事件的规则偏离，不输出疾病概率或真实世界危险概率；
- 当前活动模型使用 CAPTURE-24 恢复前缀子集，主要不是老人数据，也不是独立外部验证；
- 三个 manifest 均为 `deployment_approved=false`、`external_validation_completed=false`；
- UI 截图证明页面交付形态，不证明模型准确、校准、提前量或临床有效性；
- 未获得真实老人纵向数据、连续预事件标签、许可、伦理审批、前瞻验证和可接受误报负担前，不得声称“系统已经能提前预测老人跌倒”。

## 2. 当前仓库的现场状态

### 2.1 Git 状态（2026-08-31 现场只读核验）

| 项目 | 现场结果 |
|---|---|
| 分支 | `codex/tabler-ui-migration` |
| HEAD | `32d20fd8df4ae7d62759749cd09d043e671c0c2f` |
| HEAD 标题 | `Revert "feat: apply serenity-inspired design system"` |
| 被撤销提交 | `8b19fda94d147b1daa4ce4a02fe2b037f36cf6a2`，标题为 `feat: apply serenity-inspired design system` |
| 回退后的树 | `git diff --quiet 08ba0ad 32d20fd` 返回相同；即 `32d20fd` 的文件树恢复到 `08ba0ad` 的原版 Tabler 状态，同时保留回退历史 |
| remote | `git remote -v` 无输出；仓库未配置远程 |
| 写本文档前工作区 | `git status --porcelain=v2 --branch` 只有分支元数据，无已有改动或未跟踪文件 |

本任务只新增本文件，不提交、不推送、不创建 remote，也不改写现有历史。

### 2.2 当前软件闭环

当前平台已完成：

- Vue 3 + TypeScript + Vite 的简体中文前端；
- Tabler 浅色研究工作台、左侧导航、系统状态、案例筛选、单案例回放、三模型区域、批量回放和报告区域；
- FastAPI 本机后端，服务版本 `0.8.0`；
- SQLite 结构版本 `5`，保存来源、案例、数据质量、标签、模型 manifest、回放和可恢复批量任务；
- ONNX Runtime 执行跌倒检测与活动识别模型，统计规则执行规律异常模型；
- `WS /ws/system` 系统连接状态通道；
- 同一 FastAPI 进程在生产运行方式下提供前端、API 与 WebSocket；
- Windows 双击启动、停止、诊断脚本；
- 105 个可追溯演示案例、三个独立模型、三份保存的报告和可恢复批量回放。

这里的“闭环”指本机案例读取、模型/规则执行、结果展示、证据追溯和批量任务恢复闭环，不是实际穿戴设备、真实老人连续监护、医学预测或救援闭环。

### 2.3 数据与模型现状

#### A. 腕部跌倒检测

- manifest：`models\fall_detector\tcn_final_candidate\manifest.json`
- 版本：`0.2.0-dev`
- 格式：ONNX
- 输入：`50 Hz × 4 秒 × 6 轴 = 200 × 6`，通道为 `ax/ay/az/gx/gy/gz`
- artifact SHA-256：`1e214ebd89fce6620cf09a7a071aa904cd5ca0932e960181ce43c3695e212a3f`
- 当前任务定义：识别刚刚发生或正在发生的受控模拟跌倒候选，进入事件状态机复核；不是预测未来跌倒
- WEDA-FALL 演示案例：40 条年轻参与者受控模拟跌倒、30 条老年参与者受控日常活动、30 条年轻参与者受控日常活动
- 保存的同源回放结果：40/40 受控模拟跌倒出现与标签重叠的候选告警；60 个日常活动出现 2 段候选误报；30 个老人日常活动本次为 0 段候选误报
- 限制：这是同源行为重放，不是独立外部泛化评估；0.7 工作阈值需要外部确认；没有真实老人意外跌倒或跌倒前连续数据
- 状态：`deployment_approved=false`

#### B. 个人生活规律异常

- manifest：`models\routine_anomaly\statistical_v1\manifest.json`
- 版本：`1.0.0`
- 格式：`STATISTICAL_RULES`
- 输入：100 天的用餐、午睡、散步事件
- 数据：固定种子 `20260828` 生成的 100 天、551 条 `SYNTHETIC_ROUTINE` 事件
- artifact SHA-256：`bf3b6c31c5b4a8fde3f68e888fe4b1f3a8a6bbf470502163bf1f87ee9611f3c1`
- 保存的规则核验：基线、晚餐过晚、缺少用餐、散步过多、午睡过长五个确定性场景全部通过
- 当前输出：时间、次数、时长或缺失是否偏离合成基线；0–1 异常分数是规则距离的归一化，不是医学风险概率
- 限制：没有真实老人长期生活史训练或外部验证
- 状态：`deployment_approved=false`

#### C. 腕部活动识别

- manifest：`models\activity_recognition\capture24_linear_v1\manifest.json`
- 版本：`1.0.0`
- 格式：ONNX
- 输入：`20 Hz × 20 秒 × 3 轴 = 400 × 3` 加速度
- 输出：走路、进食候选、睡眠/躺卧候选、其他或未知
- artifact SHA-256：`47a552b23092920ffbd74c14f3de3abce2aad33771066180f31a42f6e6be667e`
- 数据：官方大文件下载中断后，只恢复并逐成员校验出 48 名参与者的完整成员；不是完整 151 人数据包
- 训练/评估：24 名训练参与者、12 名不重叠评估参与者；7,240 个训练窗口、3,728 个评估窗口
- 保存的恢复子集内参与者留出结果：准确率 `0.6075643776824035`，宏平均 F1 `0.5977935900949525`
- 限制：CAPTURE-24 以年轻参与者为主；当前结果不是老人专项结果，也不是独立外部验证；活动输出只是候选
- 状态：`deployment_approved=false`

三个模型当前必须保持独立。规律异常高、活动候选变化或跌倒候选概率，都不能直接解释为“未来数秒跌倒概率”。

### 2.4 SQLite 现场只读快照

数据库路径：`backend\runtime\smartwatch.sqlite3`。数据库位于 Git 忽略目录，不应上传或纳入公开仓库。

2026-08-31 使用 SQLite `mode=ro` 现场读取：

| 项目 | 结果 |
|---|---:|
| `PRAGMA user_version` | 5 |
| 案例总数 | 105 |
| `SIMULATED_FALL` | 40 |
| `REAL_LAB_ACTIVITY` | 60 |
| `REAL_FREE_LIVING` | 4 |
| `SYNTHETIC_ROUTINE` | 1 |
| 模型 manifest | 3 |
| 批量回放任务 | 5 |
| 已完成批量任务 | 5 |

数据库仍保存多次 105/105、失败 0 的批量回放任务，以及一条 1/1、失败 0、`recovery_count=1` 的重启恢复任务。以上证明本机回放管线与恢复记录存在，不证明模型外部泛化或提前预测有效。

### 2.5 当前 API 与 WebSocket

后端路由源：`backend\app\main.py`；数据合同源：`backend\app\contracts.py`；数据库实现：`backend\app\database.py`。

| 路由 | 当前职责 |
|---|---|
| `GET /api/health` | 服务、SQLite、案例和实时通道状态 |
| `GET /api/contracts` | 真实性类别、三模型输入输出和回放合同 |
| `GET /api/cases` | 服务端分页、筛选和白名单排序的案例目录 |
| `GET /api/models` | SQLite 中的模型版本、哈希、审批状态和限制 |
| `GET /api/reports` | 三份保存报告的规范化清单 |
| `GET /api/reports/export.json` | 下载统一报告快照 |
| `GET /api/cases/{case_id}/replay-preview` | 校验本地文件并返回真实波形、标签、窗口、候选告警或规律判断 |
| `POST /api/batch-replays` | 创建幂等、SQLite 持久化的批量回放任务 |
| `GET /api/batch-replays` | 查询批量任务清单 |
| `GET /api/batch-replays/{task_id}` | 查询单个批量任务与恢复进度 |
| `WS /ws/system` | 只传系统状态 |

当前没有提前风险预测 API、时间到事件输出、个体基线服务、真实传感器流 WebSocket、报警状态机或家属通知 API。`WS /ws/system` 不能描述为实体手表实时数据通道。

### 2.6 UI、运行方式与截图

当前 HEAD 对应的是恢复后的原版 Tabler 左侧导航工作台，而不是被撤销的 Serenity 风格。界面包含系统总览、案例与回放、三模型中心、批量回放、测试报告和数据来源；“案例库”等尚未开放的独立页面继续诚实标注为未开放。

普通用户在完成首次依赖安装后，可使用：

- `scripts\windows\start-smartwatch.cmd`：构建前端、启动只监听本机的服务并打开页面；
- `scripts\windows\stop-smartwatch.cmd`：停止本项目进程，保留 SQLite 与日志；
- `scripts\windows\diagnose-smartwatch.cmd`：只读检查依赖、构建、数据库、三个模型资产和健康接口。

生产地址固定为 `http://127.0.0.1:8000/`。开发模式命令见根目录 `README.md`。本交接编写时，`GET http://127.0.0.1:8000/api/health` 返回连接被拒绝，说明服务当时未启动；因此这里只记录运行入口，不声称平台当前在线。

已生成并现场确认存在的 UI 截图：

| 截图 | 文件大小 | SHA-256 | 证据范围 |
|---|---:|---|---|
| `C:\Users\yangxinyi\Documents\Codex\2026-08-29\final-delivery-summary-md-c-users-2\smartwatch-frontend-preview.png` | 87,858 字节 | `73DEEAB2688AEC8B60273CA01CF93112F4D262F96822DB33CF66731C93B3AD8D` | Tabler 系统总览首屏、左侧导航、105 案例和本机连接状态的 UI 交付物 |
| `C:\Users\yangxinyi\Documents\Codex\2026-08-29\final-delivery-summary-md-c-users-2\smartwatch-frontend-full-page.png` | 268,721 字节 | `F94C823F91CABBE5358DCFCDBF73E420F838A1409100297006857F52C1FFC5C2` | 同一 Tabler 页面完整长图，覆盖回放、模型、批量与报告区域的 UI 交付物 |

两张图不能作为模型准确率、提前量、真实老人适用性、校准或外部验证证据。

### 2.7 已保存验证基线与本轮核验边界

`TASK_STATE.md` 和 `FINAL_DELIVERY_SUMMARY.md` 记录的 2026-08-29 已完成验证基线包括：

- 后端 `59 passed`；
- Vue/TypeScript 类型检查与 Vite 生产构建通过；
- `npm audit` 为 0 个已知漏洞；
- `pip check` 无损坏或冲突依赖；
- OpenAPI、JSON Schema、前端 TypeScript 生成链通过；
- `designmd lint` 为 0 错误、0 警告；
- Premium 严格界面审计为 0 findings；
- 桌面 `1440×900` 与手机 `390×844` 无页面级横向溢出；
- 当时的生产页面浏览器控制台无错误或警告；
- 端口只监听 `127.0.0.1:8000`；
- Windows 启动、重复启动复用、诊断、停止、停止态诊断和重新启动流程已实测；
- 105/105 批量回放完成、失败 0，并完成一次 `recovery_count=1` 的真实重启恢复验证。

这些是保存的交付验证记录。本次文档任务没有重跑完整测试、构建或浏览器矩阵；本次新增的现场证据是 Git 状态、提交树关系、模型 manifest、API 定义、SQLite 只读快照、截图存在性/哈希，以及服务未启动的健康访问结果。

三份保存报告及本轮重算 SHA-256：

| 报告 | SHA-256 |
|---|---|
| `reports\models\fall_detector_tcn_100_cases.json` | `214B39FA4BA60D227ADD9F4D45887C1BEB33D46DB18F45D8B21095A5DDEBFEAA` |
| `reports\models\routine_anomaly_scenarios.json` | `54226940CEF5CC0087F4555B6198232B5FB77FBD23B518F7D8B3F57E10F2CA34` |
| `reports\models\activity_capture24_group_holdout.json` | `C909DA7FC848814E35D4C76842C0E677FF85D39F6B2A95FF94BC557A94080095` |

## 3. 未来提前风险预测的分层方案

### 3.1 两个时间尺度

| 层次 | 典型时间尺度 | 要回答的问题 | 可以做什么 | 不能单独做什么 |
|---|---|---|---|---|
| 长期背景风险 | 数小时、一天、数天或更长 | “这个人当前是否比自己的平常状态更脆弱？” | 调整监测敏感度、提示休息/补水/寻求陪同、帮助解释即时信号 | 不能仅凭规律异常直接发紧急跌倒警报 |
| 数秒级即时风险 | 当前至未来若干秒 | “连续动作是否正在接近失稳或危险事件？” | 在证据连续且置信度足够时触发本地预警、交互确认或保护动作 | 不能用已有跌倒后候选检测冒充提前预测 |

未来系统应输出带时间范围和不确定性的风险，而不是一个没有时间含义的“健康分数”。例如，研究合同可以表达“未来 `Δ` 秒内事件风险/危险率”，但 `Δ`、事件定义和阈值必须由数据协议与验证结果确定，不能先在 UI 中写死营销数字。

### 3.2 六层研究与产品结构

#### 第 1 层：个体基线层

目标是学习“这个老人自己的正常”，而不是只学习总体平均人群。

输入可包括经授权的长期活动节律、步行和静息片段、睡眠/躺卧代理、进食候选、外出/居家时段、设备佩戴规律、近期变化趋势与传感器质量。基线应随时间缓慢更新，但在急性异常期、住院期、设备更换或数据质量差时暂停或降权，避免把异常重新学成正常。

输出至少应区分：基线是否成熟、当前数据覆盖是否足够、哪些特征偏离、偏离持续多久、模型是否遇到训练分布外状态。

#### 第 2 层：活动/状态层

把连续传感器转换为有上下文的状态候选，例如走路、坐下/起身、静息、躺卧、进食候选、上下楼或未知状态。当前活动识别模型可以作为研究起点，但不能直接视为老人场景已验证模块。

这一层必须保留“不确定/未知”，不能把低置信度片段强行归类。活动状态为短时前兆和规律异常提供上下文，例如同样的加速度变化在走路、起身和躺卧场景下意义不同。

#### 第 3 层：规律异常层

比较近期行为与个体纵向基线，形成背景风险特征，例如活动量骤降、起居时间显著变化、进食/散步模式持续偏离、昼夜节律扰动或设备佩戴中断。

当前合成规则只能验证软件管线和解释方式。未来必须用真实纵向数据学习个体内变化，并明确区分生活安排变化、设备缺失、疾病相关变化和随机波动。

#### 第 4 层：短时动作前兆层

这是“提前数秒”的关键新能力，必须使用严格截断在事件发生前的数据。研究对象可以包括步态不稳、异常摆动、突然减速、连续补偿动作、姿态失衡、抓扶前动作或近跌倒模式，但是否可从腕部信号可靠观测必须由真实数据回答。

任何包含撞击峰值、落地后静止或事件后信号的窗口只能用于当前跌倒检测或事件确认，不能进入“预事件预测”输入，否则会产生时间泄漏。

#### 第 5 层：风险融合或时间到事件层

融合层不是把三个现有分数相加。它应接收：个体基线成熟度、背景偏离、当前活动、短时前兆、传感器质量、缺失状态和模型不确定性，并输出明确预测时域内的风险、危险率或时间到事件分布。

优先比较可解释基线、分层概率模型、序列模型和生存/时间到事件模型。模型选择要由数据量、事件稀缺性、实时延迟、校准和外部验证结果决定。训练、验证和阈值选择必须按参与者、地点和时间隔离，避免同一人的相邻窗口泄漏到不同集合。

#### 第 6 层：分级报警策略层

报警层把预测证据转成用户动作，同时负责连续证据、冷却、抑制、确认、撤销、升级和失效保护。它必须与模型评估分开：高模型分数不等于自动通知家属，更不等于自动呼叫救援。

## 4. 数据路线

### 4.1 必须补齐的数据类型

#### 正样本

- 真实老人或明确高风险人群的连续穿戴记录；
- 有可信时间戳的真实跌倒、近跌倒、明显失稳和需要扶持事件；
- 覆盖事件前、事件中和事件后的完整连续窗口，而不是只截取撞击片段；
- 记录传感器位置、设备型号、采样率、佩戴侧、辅助器具、场景和数据缺失；
- 跌倒、近跌倒、坐下过快、主动躺下、绊倒后恢复等标签必须分开，不能为了增加样本混为一个类别。

真实跌倒稀少时，可把受控模拟、临床安全诱发失稳或近跌倒数据用于预训练、特征探索或安全性测试，但必须与真实目标域分开报告，不能替代真实前瞻验证。

#### 负样本与困难负样本

- 老人真实自由生活中的普通走路、转身、起坐、弯腰、上下楼、洗漱、家务、乘车和睡眠相关动作；
- 与跌倒波形相似但无危险的剧烈动作；
- 长时连续无事件数据，用于估计每人每天误报负担；
- 设备松动、摘下、充电、断流、时间漂移和传感器异常；
- 疾病、药物、疲劳或环境变化下的正常适应行为。

只做平衡窗口分类会掩盖真实世界极低事件率。评估集必须保留连续时间和真实事件发生率。

### 4.2 连续预事件窗口和标签

每个事件至少要有：

- 统一定义的事件起点、首次失稳、撞击/着地、恢复或被协助时间；
- 事件前足够长的连续数据和明确缺失区间；
- 多个预先冻结的提前量区间，例如靠近事件的 0–2 秒、2–5 秒、5–10 秒和更长背景区间；具体边界应由研究协议确定，而不是看结果后调整；
- 标签来源和置信等级，例如视频双人复核、现场观察、老人自报、家属记录或医疗记录；
- 设备时钟、视频/观察记录与事件日志的对齐误差；
- 报警计算、传输、振动/声音和用户感知的端到端延迟。

“提前量”应从预警真正可被用户感知的时间算到预定义事件点，而不是从离线模型窗口结束时间算起。若报警发生在撞击后，必须记录为零提前或事后检测，不能算提前预警。

### 4.3 个体纵向基线

- 需要连续数周到数月、跨工作日/周末、季节、健康变化和环境变化的数据；
- 明确基线冷启动期、最低有效覆盖率和可更新条件；
- 同时保留群体先验与个人适配，防止新用户完全无模型，也防止群体平均压过个人规律；
- 记录照护安排、辅助器具、居住环境变化等经同意的上下文；
- 为漂移、换设备、换手佩戴、康复训练和急性健康变化设计基线重置或冻结规则。

### 4.4 人群、分组和外部验证数据

- 目标人群应包含真实老人和高风险人群，而不是仅用年轻人模拟；
- 训练、调参、内部测试必须按人隔离；优先 LOSO 或预注册的 GroupKFold；
- 独立外部数据应来自不同机构、不同设备或不同采集时期；
- 前瞻验证必须在模型、阈值和报警规则冻结后进行；
- 报告年龄、性别、行动能力、辅助器具、共病、设备与场景覆盖，检查亚组差异；
- 所有排除、缺失、丢包、退出和无标签片段必须可追溯。

### 4.5 隐私、伦理、许可和安全

真实老人数据接入前必须由用户或具备权限的负责人完成：知情同意、伦理审查、最小化采集、用途和保留期限、撤回与删除机制、访问控制、加密、去标识化、事件影像处理、跨机构传输和家属通知授权。

必须分别核验代码许可、数据许可和模型权重许可。当前 WEDA-FALL 许可仍为 `UNVERIFIED`；原始数据、处理数组、运行数据库和个人数据不得因代码发布而自动重新分发。任何真实设备、账号、云端、医疗或正式救援接入都属于新的授权边界。

## 5. 评估路线与通过门槛

### 5.1 核心指标

| 指标 | 必须回答的问题 | 报告要求 |
|---|---|---|
| 不同提前量的事件级召回 | 在至少提前 1、2、3、5 秒等预注册门槛下，真实事件有多少被及时覆盖？ | 按提前量分别报告事件数、召回和置信区间；不能只报窗口准确率 |
| 每人每天误报数 | 老人和家属每天会被打扰多少次？ | 报均值、中位数、分位数、连续无事件人日和亚组分布 |
| 事件级精确率 | 发出的预警中有多少对应目标事件？ | 明确事件匹配窗口、重复警报合并规则和未标注事件处理 |
| AUPRC | 在极低事件率下，模型排序是否优于基线？ | 同时给出事件发生率；不能只报 AUROC |
| 校准 | 预测为某风险水平时，实际频率是否相符？ | 可靠性图、Brier 分数或适用的校准误差；按预测时域分别评估 |
| 提前量分布 | 预警通常提前多久，是否有足够时间采取动作？ | 中位数、四分位数、尾部分布、零提前和事后检测占比 |
| 覆盖率/拒判率 | 有多少时间因缺失、未知状态或分布外输入而不输出？ | 拒判原因、传感器质量和风险性能联合报告 |
| 端到端延迟 | 从采样到用户感知需要多久？ | 设备、窗口、推理、传输、报警各阶段实测，不用理论值替代 |

还应报告事件级 F1、决策曲线或净获益、不同阈值下的误报—提前量权衡、亚组性能、设备/场景鲁棒性和报警后的用户行为结果。任何数字必须来自冻结配置的实际运行。

### 5.2 验证顺序

1. **回顾性可行性研究**：只回答信号中是否存在可重复的预事件信息，严格避免事件后泄漏；
2. **参与者隔离内部验证**：使用 LOSO 或预注册 GroupKFold，所有预处理、个人适配和阈值选择都在训练折内完成；
3. **独立外部验证**：在不同机构、人群、设备或时期验证冻结模型和阈值；
4. **前瞻静默试运行**：系统实时运行并记录“本会报警”的结果，但不向老人或家属发出实际风险警报；评估实际缺失、延迟、校准和每人每天误报；
5. **受控交互试验**：伦理和安全方案通过后，先验证本地轻提示、确认/撤销和告警疲劳；
6. **有限部署研究**：只有外部前瞻证据、可接受误报、失效保护和责任边界都满足后，才讨论更广部署。

每一阶段都应预先定义成功、停止和回退条件。不能因为内部窗口指标好看就跳过外部前瞻验证。

### 5.3 必须比较的基线

- 永不报警、总是报警等完整性基线；
- 仅用简单运动阈值或现有跌倒候选检测的基线；
- 仅用个体规律偏离的背景基线；
- 仅用短时动作信号的即时模型；
- 背景风险 + 即时风险的分层融合；
- 不同窗口、不同提前量和不同冷却策略。

模型阈值只能在训练/验证数据上选择，并在外部前瞻评估前冻结。不能在测试事件上反复调整后仍称为独立验证。

## 6. 分级报警策略

建议把“模型风险”和“用户通知”做成两个独立状态机，并把事前风险与事后跌倒确认分开。

### 6.1 建议级别

| 级别 | 典型条件 | 建议动作 | 外部通知边界 |
|---|---|---|---|
| 0 正常/证据不足 | 风险低、数据正常，或模型不确定 | 不打扰；继续监测 | 不通知 |
| 1 背景关注 | 个人规律持续偏离，但无短时前兆 | 温和提示休息、补水、使用扶手或联系照护者；可延后 | 默认不自动通知家属，更不联系救援 |
| 2 即时预警 | 短时前兆连续出现，背景或活动上下文支持 | 手表振动/声音提示“请停下并扶稳”，提供立即撤销/确认 | 只有预先授权且规则满足时才可准备升级 |
| 3 高风险升级 | 多源连续证据、用户未响应或风险持续 | 更明显的本地报警，要求确认是否需要帮助 | 家属通知必须有明确授权、超时和可撤销边界；不能直接宣称已跌倒 |
| 事后事件确认 | 现有跌倒候选、无响应、姿态/活动后续证据支持 | 单独进入“可能已发生跌倒”的确认流程 | 是否联系家属或救援是另一个经过授权的安全策略 |

### 6.2 连续证据与抑制

- 不允许单个窗口瞬时越阈值就直接外发紧急通知；
- 要求连续窗口、时间积分、状态机或多源一致性，具体规则由验证数据确定；
- 把传感器质量、佩戴状态、未知活动和分布外检测作为抑制或降级条件；
- 用迟滞阈值避免风险在边界附近反复跳变；
- 每次告警后设置冷却期，并允许在风险重新显著上升时有条件解除冷却；
- 对已确认正常、主动运动、乘车、摘表、充电和设备故障等场景建立抑制逻辑；
- 记录被抑制的告警及原因，便于安全审计，不能静默丢弃证据。

### 6.3 老人确认、撤销和家属通知

- 本地预警应使用短句、明显振动/声音和大尺寸操作，避免要求老人阅读复杂概率；
- 老人应能快速选择“我没事/撤销”“需要帮助”“误报原因”，无响应不能自动解释为跌倒；
- 撤销后仍可在短时间内保留安全观察，但不应不断重复同一告警；
- 家属通知必须基于老人事先授权、通知对象、时段、升级条件和可撤销窗口；
- 家属收到的内容应说明“系统检测到风险/未收到回应”，不能把预测当成已发生事实；
- 正式救援、医疗建议和紧急呼叫需要单独的法律、安全、人机流程和实地验证，当前仓库没有这些能力。

### 6.4 失效保护

- 无数据、低电量、断连、时间不同步、模型加载失败或输入不合同时，界面应明确显示“无法评估”，不能显示低风险；
- 不能把“没有报警”解释为“安全”；
- 本地报警应优先于依赖网络的通知，云端不可达时明确降级；
- 关键决策保存时间戳、模型版本、输入质量、阈值、状态迁移和用户响应，且遵守数据最小化；
- 模型或策略更新应可回滚，更新前后分别校准和验证；
- 设立人工安全审查、事故复盘和停用开关。

## 7. UI 风格迭代与已核实回退

只记录现场可核实的事实：

1. `8b19fda94d147b1daa4ce4a02fe2b037f36cf6a2` 曾提交 Serenity 风格设计系统；
2. 用户拒绝该视觉方向后，`32d20fd8df4ae7d62759749cd09d043e671c0c2f` 通过标准 Git revert 撤销 `8b19fda`；
3. `32d20fd` 与 `08ba0ad` 的文件树现场比较相同，说明原版 Tabler 左侧导航工作台已恢复，同时 Git 历史保留；
4. 当前 `DESIGN.md` 的设计北极星仍是“医院或研究机构里的健康监测工作台”：浅色、证据优先、密度适中、无医疗宣传式或深色霓虹大屏表达；
5. 两张 2026-08-31 已生成截图显示的也是该 Tabler 工作台；截图只属于 UI 交付证据。

未来如果为提前风险增加 UI，应延续当前 Tabler 壳和真实性表达，不重新把它包装成“已能预测”的营销大屏。风险卡必须同时显示预测时域、证据来源、数据质量、不确定性、当前级别、是否已通知以及撤销/确认状态。

## 8. 当前缺口与风险登记

| 缺口/风险 | 当前事实 | 后续控制 |
|---|---|---|
| 无真实老人跌倒前连续数据 | 当前正样本是年轻人受控模拟跌倒，老人只有日常活动 | 设计前瞻、多机构、伦理合规的数据协议；分开真实跌倒、近跌倒和模拟事件 |
| 无数秒级预事件标签 | 当前跌倒窗口允许覆盖正在/已经发生的事件 | 冻结事件点、预事件截断、时间同步与提前量标签；检查时间泄漏 |
| 规律模型是合成规则 | 100 天、551 条事件均为合成 | 收集真实个体纵向数据，建立冷启动、漂移和缺失机制 |
| 活动模型目标域不匹配 | CAPTURE-24 主要不是老人，且只恢复 48 人前缀子集 | 老人专项外部数据和真实设备验证；保留未知/拒判 |
| 事件极少、类别极不平衡 | 平衡窗口成绩不能代表真实人日 | 使用连续时间评估、AUPRC、事件级召回、每人每天误报和置信区间 |
| 校准未知 | 当前概率/异常分数不能跨模型解释 | 按预测时域校准；验证集选阈值；外部前瞻重验 |
| 告警疲劳与行为风险 | 当前没有真实报警试验 | 先静默运行，再验证分级、连续证据、冷却、确认和撤销 |
| 传感器缺失和分布外输入 | 当前 WebSocket 不传真实设备流 | 建立质量门、未知状态、降级与“无法评估”显示 |
| 数据许可与隐私 | WEDA-FALL 许可仍未核实；尚无真实个人数据流程 | 用户/负责人完成许可、伦理、同意、保留、删除和访问控制决策 |
| 医疗/救援责任 | 当前不是医疗器械或救援系统 | 不做诊断和自动救援承诺；正式流程需另行法律、安全和实地验证 |
| 远程与部署 | 当前无 Git remote、只设计为 `127.0.0.1` 本机运行 | 任何上传、云端、通知服务或公开发布都需用户明确授权 |

## 9. 关键路径索引

### 当前状态与真实性

- `FINAL_DELIVERY_SUMMARY.md`：最终本机交付要点和验证摘要
- `CONTEXT_HANDOFF.md`：当前完整接手顺序、能力与授权边界
- `TASK_STATE.md`：详细实施与实测状态
- `PRODUCT.md`：当前产品范围与三模型职责
- `DATA_AND_MODEL_NOTICE.md`：数据和模型真实性硬边界
- `README.md`：普通用户安装、运行、API 与验证命令
- `DESIGN.md`：当前 Tabler 视觉系统与内容表达约束
- `UX-CONTRACT.md`：交互、可访问性、异步、验证和响应式合同

### 后端、数据库与合同

- `backend\app\main.py`：FastAPI 路由与 `WS /ws/system`
- `backend\app\contracts.py`：Pydantic 领域合同源
- `backend\app\database.py`：SQLite 结构版本 5 与持久化逻辑
- `backend\runtime\smartwatch.sqlite3`：本机运行数据库；Git 忽略、不得公开上传
- `docs\contracts\openapi.json`：提交的 OpenAPI 快照
- `docs\contracts\domain-contract.schema.json`：提交的领域 JSON Schema
- `docs\ARCHITECTURE.md`：当前系统架构

### 模型与报告

- `models\fall_detector\tcn_final_candidate\manifest.json`
- `models\routine_anomaly\statistical_v1\manifest.json`
- `models\activity_recognition\capture24_linear_v1\manifest.json`
- `reports\models\fall_detector_tcn_100_cases.json`
- `reports\models\routine_anomaly_scenarios.json`
- `reports\models\activity_capture24_group_holdout.json`

### 前端与本机运行

- `frontend\src\App.vue`
- `frontend\src\components\AppShell.vue`
- `frontend\src\style.css`
- `scripts\windows\start-smartwatch.cmd`
- `scripts\windows\stop-smartwatch.cmd`
- `scripts\windows\diagnose-smartwatch.cmd`
- `scripts\windows\README.md`

## 10. 明确未完成项

以下内容均未完成，下一任务不得把它们写成当前能力：

- 个体长期基线学习服务与真实老人纵向基线；
- 真实老人或高风险人群的连续预事件数据集；
- 预事件标签合同、时间同步和事件点复核工具；
- 短时动作前兆模型；
- 背景风险模型；
- 风险融合或时间到事件模型；
- 数秒级风险 API、数据库表和前端展示；
- 真实设备传感器流和实时推理通道；
- 风险校准、提前量评估、每人每天误报评估；
- LOSO 完整研究、独立外部前瞻验证和静默试运行；
- 分级报警状态机、冷却/抑制、老人确认/撤销；
- 家属通知、云端服务、医疗或正式救援集成；
- 隐私伦理流程、真实数据许可和部署审批；
- GitHub remote、上传、公开发布或生产部署。

## 11. 下一步：交给第二个独立新任务制定实施计划

下一步不是直接写代码或训练，而是新建**第二个独立任务**。该任务必须先完整读取：

1. `C:\Users\yangxinyi\Documents\Codex\2026-08-27\smartwatch-health-simulator\EARLY_RISK_PREDICTION_HANDOFF.md`；
2. 本文件“关键路径索引”中的当前仓库文档；
3. 当时实时的 `git status --short --branch`、分支、HEAD、最近提交和 `git remote -v`；
4. 三个模型 manifest、三份保存报告和当时 SQLite 的只读状态。

第二个任务的唯一目标应是：在不把未来要求写成已完成功能的前提下，制定可执行的分阶段实施计划。计划至少要给出每阶段目标、输入数据、输出物、代码/数据影响范围、真实性门槛、评估指标、退出条件、依赖、风险、伦理/许可用户动作、验证命令和可回滚点；还要明确哪些阶段只允许静默研究，何时才有资格讨论真实报警。

可直接使用下面的任务描述：

> 请把“老年人 AI 模拟智能手表提前风险预测”作为一个新的独立规划任务。先读取 `C:\Users\yangxinyi\Documents\Codex\2026-08-27\smartwatch-health-simulator\EARLY_RISK_PREDICTION_HANDOFF.md` 和其中列出的当前仓库证据，再现场核验 Git 与 SQLite。只制定分阶段实施计划，不训练、不部署、不接入真实个人数据、不修改现有产品代码。计划必须把长期背景风险与数秒级即时风险分层，覆盖数据协议、预事件标签、个体基线、活动/规律/动作前兆、风险融合或时间到事件、校准、事件级指标、每人每天误报、LOSO、独立外部前瞻验证、静默试运行、分级报警与失效保护，并明确每阶段的真实性门槛、退出条件、风险、用户授权点和验证证据。

本交接只提供方向和事实边界，不自动授权第二个任务实施任何阶段。
