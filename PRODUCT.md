# 产品范围

## 产品目标

开发一个可在 Windows 电脑上直接打开的模拟智能手表风险评估平台。用户不需要阅读代码，就能上传新的六轴传感器记录，运行活动识别、跌倒候选筛查和提前 1/2/3 秒公开数据研究模型，逐秒查看风险变化、波形依据和生成结论；也可以选择 105 组公开案例和 30 组自主采集案例复核。

自行上传和公开案例共用同一套六步判断框架：检查输入、统一标准、识别动作或规律、筛查跌倒动作、分析提前风险、生成结果分析。两条路径只在数据来源和实际可用通道上不同；输入条件不满足的模型必须显示“数据不足”或“未运行”及原因，不得用零分或静态结论代替。

## 目标用户

- 比赛评委：需要在几分钟内看懂系统做了什么、数据从哪里来、哪些结论已经实测。
- 项目成员：需要用简体中文界面和教学文档边学习边开发。
- 后续研究人员：需要复现实验并检查参与者分组、防泄漏、模型版本和案例来源。

## 当前范围

- Windows 桌面程序；
- Vue 3 + TypeScript 前端；
- 已接入 FastAPI、SQLite、WebSocket 和 ONNX Runtime；
- 四个研究模块的独立结果和可追溯事件；
- 100 天合成生活规律和 100 个公开来源测试案例；
- 本地优先、离线演示。

## 不在当前范围

- 实体智能手表、ESP32、MPU6050、OLED；
- 医疗诊断；
- 正式救援服务；
- 未经数据支持的未来跌倒预测；
- 把三个未经校准的概率相加成医学风险分数；
- 把模拟数据伪装成真实用户记录。

## 四个研究模块职责

| 模型 | 任务 | 首版输入 |
|---|---|---|
| 腕部跌倒检测 | 识别刚发生的受控模拟跌倒候选 | 50 Hz、4 秒、200×6 六轴 IMU |
| 个人规律异常 | 判断用餐、午睡、散步是否偏离个人长期规律 | 多日生活事件 |
| 腕部活动识别 | 识别走路、进食候选、睡眠/躺卧候选 | 20 Hz、20 秒、400×3 加速度 |
| 提前风险公开代理基线 | 评估公开受控模式是否接近未来 1、2、3 秒内的数据集跌倒区间开始 | 50 Hz、1 秒、50×6 六轴 IMU |

## 已完成基础阶段

- 独立 Git 仓库存在；
- 前端可以安装、构建、运行；
- 有一个明确标记“暂无数据”的虚拟手表首页；
- 三模型位置、输入和当前状态清楚；
- 有 `DESIGN.md`、`UX-CONTRACT.md`、`TASK_STATE.md` 和真实性声明；
- Tabler 前端工作台已完成；
- FastAPI、SQLite 和 WebSocket 已形成最小本地状态闭环；
- 统一来源、案例、传感器窗口、三模型输出、回放事件和模型 manifest 合同已建立；
- SQLite 已迁移到结构版本 5，并提供 105 个案例的服务端分页接口和可恢复批量回放；
- OpenAPI、领域 JSON Schema 和前端 TypeScript 类型可以重复生成并检查漂移；
- 已核验接入跌倒 ONNX、100 天合成规律统计规则与 CAPTURE-24 恢复前缀子集活动 ONNX；三个模型都未完成外部验证且不获部署批准。
- 总览页可从 SQLite 读取 105 个案例和三个模型，按真实性筛选，并对 WEDA 六轴、CAPTURE-24 三轴或 100 天合成规律执行只读回放；波形、标签、候选结果、质量标记和模型依据均来自保存的数据。
- 可按当前真实性筛选创建 SQLite 持久化批量任务；中断项在重启后恢复，三个模型结果保持独立。
- Windows 一键脚本可自动构建并用单一本机进程提供前端、API 与 WebSocket，同时提供停止和诊断入口。
- 提前风险研究的 P0+P1+P2 E0 基础仍保留：产品/目标合同、数据/标签合同、当前资产审计、严格预事件截断、事件级指标与 dry-run 状态机可重复运行。现实环境预测、个体长期基线、正式风险 UI、真实通知和部署 Gate 仍未通过。
- 公开数据提前风险研究基线已经实际训练并保存：只读取代理锚点前的因果窗口，按参与者隔离训练、校准和最终评估，分别输出 1/2/3 秒研究分数；模型与评估报告可重复生成。该能力只对应 WEDA 受控数据集的跌倒区间开始代理标签，不是现实意外跌倒预测证据。
- `127.0.0.1:8010` 新增“新数据检测”：标准 CSV/JSON 经六轴校验、单位换算和 50 Hz 重采样后，在本次请求中真实运行活动识别、跌倒候选和提前风险模型；页面显示实际波形、逐秒风险、模型阈值、格式化依据和最终结论，上传内容不落盘。
- 公开案例与新数据检测已统一为同一组六步状态和同一结果阅读顺序：结论先看、关键实测依据、各模型如何参与、交叉核对过程、最终结论和适用边界。WEDA 六轴案例运行满足条件的三个传感器模型；CAPTURE-24 三轴活动案例真实运行活动识别模型，并明确说明另外两个模型因缺少陀螺仪未运行；合成规律案例真实运行规律规则，且不伪造传感器波形。
- 自主采集登记已经建立但故意保持 `0/约30组`。收到真实文件前，不启用“已使用自主采集数据完成工程验证”的最终校赛表述。
