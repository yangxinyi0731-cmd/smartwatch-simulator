# 数据与模型真实性声明

本项目是研究和比赛演示软件，不是医疗器械、医疗诊断系统或正式救援系统。

P0–P2 的确定性 E0 夹具仍只验证公式和状态机。除此之外，校赛工作台现在增加了一个真实训练和运行的“公开数据代理提前风险基线”：它只读取 WEDA-FALL 代理锚点前的 1 秒六轴窗口，按参与者隔离训练、校准和评估，并分别输出 1/2/3 秒研究分数。这个模型证明的是“公开受控数据中存在可被当前方法学习的代理模式”，不是现实环境中的个人跌倒概率，也不是经专家标注的真实 `t_instability`。因此服务继续保持 `prediction_evidence=false`、`deployment_approved=false`、`real_world_prediction_evidence=false`，真实外部通知数必须为 0。

上传新数据时，文件只在本机进程内存中分析：校验时间和六轴、换算单位、重采样到 50 Hz，再实际运行活动识别、跌倒候选和公开代理提前风险模型。页面结论必须来自本次计算结果，不能在前端预写；普通上传内容不自动保存或计入自主采集。P01 自主采集批次已单独完成登记：1 名匿名参与者、6 类受控非跌倒动作、30 组记录全部通过质量与冻结模型工程验证。该结果只启用“使用自主采集数据完成数据接入和工程验证”的表述，不启用现实跌倒预测、跨参与者泛化或医疗结论。

必须遵守：

1. 随机生成的 100 天生活规律标记为“合成数据”；
2. WEDA-FALL 跌倒标记为“年轻参与者受控模拟跌倒”；
3. 老年参与者的 WEDA-FALL 数据只用于日常活动误报分析；
4. WISDM 受控活动不能描述为老年人自由生活；
5. ExtraSensory 以年轻参与者为主，不能描述为老年人数据；
6. CAPTURE-24 自由生活腕部数据不等于真实老人跌倒，也不能描述为老人专项活动数据；本次只使用逐成员校验的 48 人恢复前缀子集，不得称为完整 151 人数据包；
7. 缺少陀螺仪的案例不得用全零伪造六轴输入；
8. 所有指标必须来自保存了配置、分组和命令的实际运行；
9. 未完成外部验证的模型保持 `deployment_approved=false`；
10. 公开下载不自动等于允许重新上传或商用，代码许可与数据许可分别核验。
11. `reports/early_risk/` 的确定性夹具指标固定为 E0，只验证公式与软件，不能描述为真实老人提前量、召回、精确率、AUPRC、校准或误报/人日；
12. P0–P2 的策略代码只允许 dry-run，禁止振动、声音、家属消息、救援或任何真实外部通知。
13. WEDA 的 `FALL_INTERVAL` 开始只能称为“数据集跌倒区间开始代理锚点”，不得改写为真实失稳起点；
14. 1/2/3 秒评估必须同时公开命中数和符合精确提前时间条件的分母，不得只展示百分比；
15. 新数据上传结果必须分别显示质量、活动候选、跌倒筛查、三个提前时间分数和判断依据，不得生成无法追溯的综合风险分；
16. `data/catalog/self_collected_pending_v1.json` 只登记真实收到且通过质量核验的自主采集文件；公开数据、合成数据或重复记录不得冒充自主采集。当前 P01 的30组记录均以匿名编号保存，原始设备信息和绝对采集时间不进入 GitHub。
17. P01 的30组记录全部来自同一参与者，动作标签来自压缩包文件名；它们可支持数据接入与冻结模型工程验证，但不能证明跨参与者泛化能力。

界面真实性类别：

- `REAL_FREE_LIVING`：真实自由生活记录；
- `REAL_LAB_ACTIVITY`：真实参与者受控活动；
- `SIMULATED_FALL`：受控模拟跌倒；
- `SYNTHETIC_ROUTINE`：程序生成的生活规律；
- `DERIVED_PERTURBATION`：真实数据上的明确派生异常。
