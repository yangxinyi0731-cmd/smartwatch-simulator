# 模拟手表校赛风险研究版：完整交接

更新时间：2026-09-01

实际仓库：`C:\Users\yangxinyi\Documents\Codex\2026-08-27\smartwatch-health-simulator`

当前分支：`codex/early-risk-e0-workbench`

本轮开发起点：`b447dd0ef99490198c37813bad2f3c36d6777eff`

本机页面：`http://127.0.0.1:8010/`

## 1. 当前结论

校赛版已经从“跌倒检测加产品介绍”推进为一个可实际操作的风险研究工作台：

1. 公开 WEDA-FALL 六轴数据已经训练出独立的 1 秒、2 秒、3 秒代理提前风险模型；
2. 页面新增“新数据检测”，选择标准 CSV/JSON 后会在本机真实完成格式检查、单位换算、50 Hz 重采样、活动识别、跌倒候选筛查、提前风险计算和文字解释；
3. 结果不再合成模糊的“综合风险分”，而是分开显示数据质量、当前动作、跌倒动作筛查、主要判断依据、最终结论、实际波形和逐秒风险曲线；
4. 105 组原有案例仍可回放，其中六轴 WEDA 案例会在请求时重新运行跌倒模型和提前风险模型；
5. 自主采集数据位置已经建好，但严格保持 `0 / 约30组`、`cases=[]`，没有用程序生成的数据冒充人工采集；
6. 外部通知、真实个人预测、现实报警和部署审批仍全部关闭。

当前最准确的表述是：

> 已完成公开受控数据代理模型、新文件自动分析链路、波形证据、逐秒 1/2/3 秒研究输出和可操作演示页面；自主采集工程验证等待真实文件接入。

## 2. 评委看到的完整操作路径

```text
选择六轴 CSV / JSON
        ↓
检查时间、加速度和陀螺仪
        ↓
换算为 50 Hz、m/s²、rad/s
        ↓
运行腕部活动识别模型
        ↓
运行跌倒动作候选模型
        ↓
运行 1 / 2 / 3 秒代理提前风险模型
        ↓
显示实际波形、逐秒曲线、判断依据和结论
```

该链路由 `POST /api/analyze-upload` 提供，不是前端预先写死一个结论。上传文件只进入当前服务进程内存，不写入 SQLite，不加入案例库，刷新后清除。

## 3. 公开代理提前风险模型

模型目录：`models/early_risk/public_weda_linear_v1/`

训练入口：

```powershell
.\.venv\Scripts\python.exe -m research.early_risk.train_public_risk_baseline
```

核心约束：

- 固定输入为 `50 Hz × 1 秒 × 6 轴 = 50×6`；
- 每个时间点只读取该时间点及其之前的信号；
- 训练、阈值校准和最终评估参与者完全隔离；
- 阈值只在校准组确定，不用最终评估组挑阈值；
- 三个输出分别对应 1 秒、2 秒、3 秒代理标签研究分数；
- 页面强制说明它们不是现实个人跌倒概率。

训练、校准、评估拆分：

| 拆分 | 参与者数 | 案例数 | 窗口数 |
| --- | ---: | ---: | ---: |
| 训练 | 13 | 52 | 1840 |
| 校准 | 4 | 19 | 584 |
| 独立参与者评估 | 8 | 29 | 1130 |

公开模型在最终参与者隔离评估组的“恰好提前该秒数”快照：

| 代理提前时间 | 达到各自阈值 | 可评估受控跌倒案例 | 当前快照召回 |
| --- | ---: | ---: | ---: |
| 1 秒 | 9 | 11 | 81.8% |
| 2 秒 | 10 | 11 | 90.9% |
| 3 秒 | 7 | 10 | 70.0% |

这些数字只能解释为：在固定公开受控数据、固定代理锚点和当前参与者隔离拆分下的研究快照。它们不是现实跌倒准确率，也不是老人专项结果。

完整报告：`reports/early_risk/public_weda_linear_v1.json`

模型卡：`models/early_risk/public_weda_linear_v1/MODEL_CARD.md`

### 代理锚点必须这样解释

当前时间锚点是 WEDA-FALL 数据集标注的“受控跌倒区间开始”，代码为 `WEDA_FALL_INTERVAL_START`。它不等同于专家确认的真实失稳起点 `t_instability`。因此页面和报告都使用“公开数据代理提前风险”，不能写成“已经在现实中提前三秒预测真实跌倒”。

## 4. 新数据检测的输入与输出

标准格式说明：`docs/early_risk/NEW_DATA_FORMAT.md`

最简单的 CSV 表头：

```csv
time_s,ax,ay,az,gx,gy,gz
0.00,0.10,0.20,9.70,0.01,0.02,0.03
0.02,0.12,0.18,9.72,0.02,0.02,0.04
```

界面允许用户明确选择：

- 加速度：`m/s²` 或 `g`；
- 角速度：`rad/s` 或 `deg/s`。

服务端会检查：

- 文件必须是 UTF-8 CSV 或 JSON，且不超过 5 MB；
- 时间必须可排序、可去重，并能得到 5–400 Hz 的合理源采样率；
- 六轴值必须是有限数值；
- 至少包含 1 秒数据；
- 统一重采样到 50 Hz，再把同一份标准数组交给模型。

不同模型需要的最短时长不同：

- 提前风险：至少 1 秒；
- 跌倒候选：至少 4 秒；
- 活动识别：至少 20 秒。

时长不足时页面会明确标记该模型“数据不足”，不会伪造输出。

结果结构固定为：

- 数据质量；
- 当前动作候选；
- 跌倒动作筛查；
- 文件是否保存；
- 主要判断依据；
- 最终结论；
- 实际加速度/角速度合量波形；
- 1 秒、2 秒、3 秒逐秒代理风险曲线和各自阈值。

## 5. 自主采集数据最后一步

当前登记文件：`data/catalog/self_collected_pending_v1.json`

当前状态：

- 目标：约 30 组；
- 已收到：0 组；
- 已导入：0 组；
- `claim_enabled=false`；
- 页面固定显示 `0 / 约30组`。

收到真实文件后，剩余工作被压缩为以下一步：

1. 保留原始文件并计算 SHA-256；
2. 为每段记录确认参与者编号、动作名称、开始/结束和是否受控；
3. 运行现有新数据检测链路并生成质量结果；
4. 按参与者隔离做工程验证，防止同一个人的数据同时进入调参和验证；
5. 将通过复核的文件登记进自主采集目录和演示案例；
6. 冻结最终工程验证报告，再把页面计数从 0 更新为实际数量。

不需要重新设计页面，也不需要重新发明文件标准和推理接口。

## 6. 最终对外表述的启用条件

用户希望最终证明：

> 我们已经基于公开腕部传感器数据建立了动作识别和跌倒候选筛查模型，并使用自主采集数据对模型进行工程验证；系统能够自动分析新的传感器记录，展示波形证据并生成风险判断，后续可将同一套模型和数据标准迁移到真实智能手表。

目前这句话的前半部分、新文件自动分析、波形和判断已经有程序证据；“并使用自主采集数据对模型进行工程验证”仍缺真实文件，因此整句话当前不能作为已完成事实发布。收到并验证真实文件后，才允许把 `claim_enabled` 改为 `true`。

另外，WEDA-FALL 固定来源仓库当前没有确认到明确的开源许可证文件。校赛材料可以准确写“公开可访问数据”，但在向外分发原始数据或正式发布前仍需要补许可证/授权核验。

## 7. 启动与验证

最简单启动：

```text
双击 scripts\windows\start-early-risk-workbench.cmd
```

地址：`http://127.0.0.1:8010/`

诊断：

```text
双击 scripts\windows\diagnose-early-risk-workbench.cmd
```

停止：

```text
双击 scripts\windows\stop-early-risk-workbench.cmd
```

本轮最终验证：

- Python 完整回归：`105 passed`；
- 前端 TypeScript 检查与 Vite 生产构建：通过；
- JavaScript 语法：通过；
- OpenAPI TypeScript 重新生成：通过且无漂移；
- npm audit：0 个已知漏洞；
- pip check：无损坏依赖；
- `DESIGN.md` lint：0 错误、0 警告；
- Premium 严格审计：0 findings；
- Windows 工作台诊断：全部通过；
- Windows 一键脚本：正在运行时重复启动可安全复用，停止后端口释放，再启动可恢复；
- 应用内浏览器：无效文件拦截、38.66 秒六轴 CSV 全链路分析、公开案例回放、桌面和 390×844 窄屏均通过；
- 页面级横向溢出：0；
- 浏览器控制台错误/警告：0；
- 外部通知：0。

机器可读验收：`reports/early_risk/school_demo_verification.json`

## 8. Git 与发布状态

- 当前分支：`codex/early-risk-e0-workbench`；
- 本轮起点：`b447dd0ef99490198c37813bad2f3c36d6777eff`；
- 没有配置 Git remote；
- 没有向 GitHub 或其他平台推送；
- 页面只监听 `127.0.0.1:8010`，当前不是互联网部署。
